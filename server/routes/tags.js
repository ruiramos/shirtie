var request  = require('request'),
    qs       = require('querystring'),
    config   = require('../config.json'),
    // clarifai = require('./clarifai_node.js');

// Initialize clarifai SDK
// clarifai.initAPI( config.clarifai_id, config.clarifai_secret );

var getTags = function(req, res, next){

};